{
  "name": "@schede/workout-execution",
  "version": "1.0.0",
  "private": true,
  "description": "Deterministic workout execution, autoregulation and progression engine",
  "main": "./dist/packages/workout-execution/src/index.js",
  "types": "./dist/packages/workout-execution/src/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@schede/exercise-selection": "workspace:*",
    "@schede/workout-generation": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "~5.6.3",
    "vitest": "~2.1.6"
  }
}
