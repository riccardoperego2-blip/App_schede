{
  "name": "@schede/workout-generation",
  "version": "1.0.0",
  "private": true,
  "description": "Deterministic mesocycle + session plan generation (MEV/MAV/MRV, SRA, deload)",
  "main": "./dist/packages/workout-generation/src/index.js",
  "types": "./dist/packages/workout-generation/src/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@schede/exercise-selection": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "~5.6.3",
    "vitest": "~2.1.6"
  }
}
